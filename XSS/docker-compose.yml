version: '3.8'

services:
  xss-demo:
    build: .
    container_name: xss-demo-app
    ports:
      - "5000:5000"
    environment:
      - FLASK_ENV=development
    volumes:
      - .:/app
    stdin_open: true
    tty: true
